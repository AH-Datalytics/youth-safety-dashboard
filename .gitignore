# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Source data (downloaded from OneDrive at runtime)
data/source/
data/crosswalks/*
# ...except the geocode cache, which is derived from public school addresses
# and is NOT in the OneDrive download list. Without it committed, the campus
# ETL emits schools with lat/lon 0 and the school dot map renders empty.
!data/crosswalks/school-geocodes.json

# Temp files from ETL
data/generated/_*

# Uncompressed JSON (too large for GitHub, only .json.gz needed)
data/generated/**/*.json
!data/generated/.gitkeep

# Parquet files (generated at CI time)
data/generated/**/*.parquet

# Git worktrees
.worktrees/
